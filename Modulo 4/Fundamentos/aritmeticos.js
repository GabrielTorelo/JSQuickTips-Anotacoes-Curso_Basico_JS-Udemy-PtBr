const [a, b, c, d, e] = [3, 5, 1, 15] // Criando uma constante usando o operador destructuring

const soma = a + b + c + d
console.log(soma)

const subtracao = d - b
console.log(subtracao)

const mult = a * b
console.log(mult)

const divisao = d / a
console.log(divisao)

const mod = a % 2 // Armazenando na váriavel 'mod' o resto da divisão da variável 'a' por 2, ou seja, está dividindo a variável 'a' por 2 e armazenando o valor do resto da divisão na variável 'mod'
console.log(mod)

console.log(-divisao) // Podemos aplicar um operador unário nas variávies, nesse caso mudando o valor da variável 'divisão' para negativo